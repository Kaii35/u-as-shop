# Variables del docker-compose y del frontend.
# Copia a .env. Los valores de abajo sirven tal cual para desarrollo.

POSTGRES_USER=aurelle
POSTGRES_PASSWORD=aurelle_dev
POSTGRES_DB=aurelle
POSTGRES_PORT=5436

API_PORT=4100
JWT_SECRET=cambiar-en-produccion
CORS_ORIGIN=http://localhost:5173

# Base de la API que consume el navegador. En desarrollo se asume
# http://localhost:4100 si se deja vacia.
VITE_API_URL=
